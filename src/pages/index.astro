---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

// Fetch wines sorted by rating (highest first)
const allWines = (await getCollection('work')).sort((a, b) =>
    (b.data.rating || 0) - (a.data.rating || 0)
);
const featuredWine = allWines[0];  // Leonidas 98pts
const collectionWines = allWines.slice(1, 4); // next 3

// Fetch events
const events = (await getCollection('news')).sort((a, b) =>
    new Date(b.data.date || 0).getTime() - new Date(a.data.date || 0).getTime()
).slice(0, 3);
---

<Layout title="Maison" description="Estate wines of exceptional character from Napa Valley and Sonoma Coast.">

    <!-- ── HERO ──────────────────────────────────────────────── -->
    <section class="hero">
        <div class="hero-bg" id="hero-bg"></div>
        <div class="hero-overlay"></div>
        <div class="hero-content">
            <p class="hero-eyebrow">Est. 1989 &nbsp;·&nbsp; Napa Valley</p>
            <h1 class="hero-title">Maison</h1>
            <div class="hero-rule"></div>
            <p class="hero-desc">
                We are proud to share our remarkable limited production wines, whose subtle balance of elegance, structure, power and finesse are a striking embodiment of everything we hold dear about this land.
            </p>
            <div class="hero-actions">
                <a href="/contact" class="btn btn-gold">Join the Allocation List</a>
                <a href="/work" class="btn btn-outline-white">Explore the Wines</a>
            </div>
        </div>
        <a href="#quote-section" class="hero-scroll" aria-label="Scroll down">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><polyline points="6 9 12 15 18 9"></polyline></svg>
        </a>
    </section>

    <!-- ── QUOTE CAROUSEL ──────────────────────────────────── -->
    <section id="quote-section" class="quote-section">
        <div class="quote-inner">
            <div class="progress-ring-wrap" aria-hidden="true">
                <svg class="progress-ring" width="52" height="52" viewBox="0 0 52 52">
                    <circle class="progress-ring__bg" cx="26" cy="26" r="22" />
                    <circle class="progress-ring__circle" cx="26" cy="26" r="22" />
                </svg>
                <span class="progress-ring__count" id="quote-count">1</span>
            </div>
            <ul class="quote-list" id="quote-list" aria-live="polite">
                <li class="quote-item revealed" data-index="0">
                    <blockquote class="quote-text">"Opens with gregarious blue and black fruits, dried herbs, chargrill and savory notes. The full-bodied palate is vibrant, expressive, mineral, and earthy with black fruits and firm, grainy tannins, finishing long."</blockquote>
                    <cite class="quote-cite">
                        <span class="quote-score">98 Points</span>
                        <span class="quote-source">Robert Parker · Wine Advocate</span>
                        <span class="quote-wine">Leonidas Cabernet Sauvignon 2020</span>
                    </cite>
                </li>
                <li class="quote-item" data-index="1">
                    <blockquote class="quote-text">"Extraordinary complexity and finesse. A wine of great poise and elegance, with a crystalline purity of fruit that is genuinely rare in today's market. Among the finest Napa Valley wines I have encountered in years."</blockquote>
                    <cite class="quote-cite">
                        <span class="quote-score">97 Points</span>
                        <span class="quote-source">James Suckling</span>
                        <span class="quote-wine">Leonidas Cabernet Sauvignon 2018</span>
                    </cite>
                </li>
                <li class="quote-item" data-index="2">
                    <blockquote class="quote-text">"Mountain intensity with classical restraint. Each sip reveals something new — a testament to both terroir and the winemaker's art. Among the finest expressions from any appellation I have encountered."</blockquote>
                    <cite class="quote-cite">
                        <span class="quote-score">96 Points</span>
                        <span class="quote-source">Wine Spectator</span>
                        <span class="quote-wine">Howell Mountain Hillside Blend 2019</span>
                    </cite>
                </li>
                <li class="quote-item" data-index="3">
                    <blockquote class="quote-text">"Effortlessly beautiful. The kind of Pinot Noir that reminds you why the grape is called the heartbreak grape — seductive and elusive in equal measure. A remarkable achievement from an already remarkable estate."</blockquote>
                    <cite class="quote-cite">
                        <span class="quote-score">94 Points</span>
                        <span class="quote-source">Vinous</span>
                        <span class="quote-wine">Estate Pinot Noir 2021</span>
                    </cite>
                </li>
            </ul>
            <div class="quote-dots" id="quote-dots">
                <button class="quote-dot active" data-slide="0" aria-label="Quote 1"></button>
                <button class="quote-dot" data-slide="1" aria-label="Quote 2"></button>
                <button class="quote-dot" data-slide="2" aria-label="Quote 3"></button>
                <button class="quote-dot" data-slide="3" aria-label="Quote 4"></button>
            </div>
        </div>
    </section>

    <!-- ── PHILOSOPHY STATEMENT ───────────────────────────── -->
    <section class="statement-section">
        <div class="statement-inner" data-reveal="up">
            <span class="eyebrow">The Philosophy</span>
            <h2 class="statement-heading">
                We do not chase scores,<br>
                <em>though the scores arrive anyway.</em>
            </h2>
            <p class="statement-body">
                For over three decades, Maison has been guided by a single conviction: the finest wines come from the finest farming. Our 42 acres of estate vines are worked by hand, farmed biodynamically, and harvested over multiple passes to capture only what the year has given us. Nothing more.
            </p>
            <a href="/bio" class="text-link">Read our story <span aria-hidden="true">→</span></a>
        </div>
    </section>

    <!-- ── FEATURED WINE ──────────────────────────────────── -->
    {featuredWine && (
        <section class="feature-section">
            <div class="feature-img-wrap" data-reveal="left">
                <div class="feature-img-inner">
                    <img
                        src={featuredWine.data.thumbnail || '/img/wine-red-glass.jpg'}
                        alt={featuredWine.data.title}
                        class="feature-img"
                    />
                </div>
                {featuredWine.data.rating && (
                    <div class="feature-score-badge" aria-label={`${featuredWine.data.rating} points`}>
                        <span class="feature-score-num">{featuredWine.data.rating}</span>
                        <span class="feature-score-pts">pts</span>
                    </div>
                )}
            </div>
            <div class="feature-content" data-reveal="right">
                <span class="eyebrow">Flagship Wine</span>
                <h2 class="feature-title">{featuredWine.data.title}</h2>
                <p class="feature-meta">
                    {featuredWine.data.vintage && <span>{featuredWine.data.vintage}</span>}
                    {featuredWine.data.appellation && <span>{featuredWine.data.appellation}</span>}
                </p>
                <div class="feature-rule"></div>
                <p class="feature-desc">{featuredWine.data.description}</p>
                {featuredWine.data.critic && featuredWine.data.rating && (
                    <p class="feature-critic">
                        <strong>{featuredWine.data.rating} Points</strong> — {featuredWine.data.critic}
                    </p>
                )}
                <a href={`/work/${featuredWine.slug}`} class="feature-link">
                    Discover this wine <span aria-hidden="true">→</span>
                </a>
            </div>
        </section>
    )}

    <!-- ── WINE COLLECTION ────────────────────────────────── -->
    <section class="collection-section">
        <div class="collection-header" data-reveal="up">
            <h2 class="collection-title">The Collection</h2>
            <a href="/work" class="text-link">View all wines <span aria-hidden="true">→</span></a>
        </div>
        <div class="collection-grid">
            {collectionWines.map((wine, i) => (
                <button
                    type="button"
                    class="collection-card"
                    data-reveal="up"
                    data-delay={String(i + 1)}
                    data-wine-slug={wine.slug}
                    data-wine-title={wine.data.title}
                    data-wine-varietal={wine.data.varietal || ''}
                    data-wine-vintage={wine.data.vintage || ''}
                    data-wine-appellation={wine.data.appellation || ''}
                    data-wine-rating={String(wine.data.rating || '')}
                    data-wine-critic={wine.data.critic || ''}
                    data-wine-price={wine.data.price || ''}
                    data-wine-description={wine.data.description || ''}
                    data-wine-img={wine.data.thumbnail || '/img/wine-red-glass.jpg'}
                    data-wine-notes={JSON.stringify(wine.data.tasting_notes || [])}
                >
                    <div class="collection-card-img-wrap">
                        <img
                            src={wine.data.thumbnail || '/img/wine-red-glass.jpg'}
                            alt={wine.data.title}
                            class="collection-card-img"
                        />
                        <div class="collection-card-overlay"></div>
                        <div class="collection-card-cta">View Details</div>
                    </div>
                    <div class="collection-card-body">
                        {wine.data.varietal && <span class="collection-card-varietal">{wine.data.varietal}</span>}
                        <h3 class="collection-card-title">{wine.data.title}</h3>
                        <div class="collection-card-footer">
                            <span class="collection-card-vintage">{wine.data.vintage}</span>
                            {wine.data.rating && <span class="collection-card-score">{wine.data.rating} pts</span>}
                        </div>
                    </div>
                </button>
            ))}
        </div>
    </section>

    <!-- ── ESTATE PARALLAX ────────────────────────────────── -->
    <section class="parallax-section" id="parallax-section">
        <div class="parallax-bg" id="parallax-bg"></div>
        <div class="parallax-overlay"></div>
        <div class="parallax-content" data-reveal="scale">
            <span class="eyebrow eyebrow-light">The Estate</span>
            <h2 class="parallax-title">
                42 Acres.<br>
                Five Blocks.<br>
                <em>One Vision.</em>
            </h2>
            <div class="parallax-rule"></div>
            <p class="parallax-text">
                Our land was chosen for its volcanic soils, its elevation, and its capacity to produce something that could not come from anywhere else on earth.
            </p>
            <a href="/vineyards" class="btn btn-outline-gold">Explore the Vineyards</a>
        </div>
    </section>

    <!-- ── EVENTS ─────────────────────────────────────────── -->
    <section class="events-section">
        <div class="events-header" data-reveal="up">
            <h2 class="events-title">Experience</h2>
            <a href="/news" class="text-link">All events <span aria-hidden="true">→</span></a>
        </div>
        <div class="events-grid">
            {events.map((event, i) => (
                <a href={`/news/${event.slug}`} class="event-card" data-reveal="up" data-delay={String(i + 1)}>
                    <div class="event-card-img-wrap">
                        <img
                            src={event.data.thumbnail || '/img/wine-tasting.jpg'}
                            alt={event.data.title}
                            class="event-card-img"
                        />
                    </div>
                    <div class="event-card-body">
                        <span class="event-card-label">
                            {event.data.date ? new Date(event.data.date).toLocaleDateString('en-US', { month: 'long', year: 'numeric' }) : 'Estate Event'}
                        </span>
                        <h3 class="event-card-title">{event.data.title}</h3>
                        <p class="event-card-desc">{event.data.description}</p>
                        <span class="event-card-link">Read more <span aria-hidden="true">→</span></span>
                    </div>
                </a>
            ))}
        </div>
    </section>

    <!-- ── WINE DETAIL MODAL ──────────────────────────────── -->
    <div id="wine-modal" class="wine-modal" aria-hidden="true" role="dialog" aria-modal="true" aria-label="Wine Details">
        <div class="wine-modal-backdrop"></div>
        <div class="wine-modal-panel">
            <button class="wine-modal-close" aria-label="Close modal">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>
            </button>
            <div class="wine-modal-img-wrap">
                <img id="modal-img" src="" alt="" class="wine-modal-img" />
                <div class="wine-modal-img-fade"></div>
                <div class="wine-modal-score-badge">
                    <span class="wine-modal-score-num" id="modal-score-num"></span>
                    <span class="wine-modal-score-pts">pts</span>
                </div>
            </div>
            <div class="wine-modal-content">
                <span class="wine-modal-varietal" id="modal-varietal"></span>
                <h2 class="wine-modal-title" id="modal-title"></h2>
                <p class="wine-modal-meta" id="modal-meta"></p>
                <div class="wine-modal-rule"></div>
                <p class="wine-modal-desc" id="modal-desc"></p>
                <ul class="wine-modal-notes" id="modal-notes"></ul>
                <div class="wine-modal-footer">
                    <span class="wine-modal-price" id="modal-price"></span>
                    <a class="wine-modal-link" id="modal-link" href="#">
                        Discover this wine <span class="wine-modal-arrow">→</span>
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- ── JOIN THE LIST ──────────────────────────────────── -->
    <section class="join-section">
        <div class="join-inner" data-reveal="scale">
            <span class="eyebrow eyebrow-light">Limited Allocation</span>
            <h2 class="join-title">Join the List</h2>
            <div class="join-rule"></div>
            <p class="join-text">
                Our wines are released annually to allocation list members before reaching retail or restaurant channels. Membership is complimentary and carries no minimum purchase obligation.
            </p>
            <a href="/contact" class="btn btn-gold-large">Request an Allocation</a>
        </div>
    </section>

</Layout>

<style>
/* ─── GLOBAL SHARED ELEMENTS ──────────────── */
.eyebrow {
    font-family: var(--font-sans-serif);
    font-size: 1rem;
    letter-spacing: 4.5px;
    text-transform: uppercase;
    color: var(--color-primary);
    display: block;
    margin-bottom: 1.8rem;
    opacity: 0.9;
}
.eyebrow-light { color: rgba(231,200,116,0.85); }

.text-link {
    font-family: var(--font-sans-serif);
    font-size: 1.05rem;
    letter-spacing: 2.5px;
    text-transform: uppercase;
    color: var(--color-base);
    text-decoration: none;
    opacity: 0.5;
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    position: relative;
    padding-bottom: 4px;
    transition: opacity 0.25s, color 0.25s, gap 0.35s cubic-bezier(0.2,0,0,1);
}
.text-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 1px;
    background: var(--color-primary);
    transform: scaleX(0.25);
    transform-origin: left;
    transition: transform 0.4s cubic-bezier(0.2,0,0,1);
}
.text-link:hover { opacity: 0.9; color: var(--color-primary); text-decoration: none; gap: 1rem; }
.text-link:hover::after { transform: scaleX(1); }

/* ── All CTAs: elegant text with animated underline, no rectangles ── */
.btn {
    font-family: var(--font-sans-serif);
    font-size: 1rem;
    letter-spacing: 3.5px;
    text-transform: uppercase;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 0.8rem;
    position: relative;
    padding-bottom: 6px;
    transition: gap 0.4s cubic-bezier(0.2,0,0,1), color 0.28s, opacity 0.28s;
}
.btn::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 1px;
    transform: scaleX(0.3);
    transform-origin: left;
    transition: transform 0.45s cubic-bezier(0.2,0,0,1);
}
.btn:hover { text-decoration: none; gap: 1.4rem; }
.btn:hover::after { transform: scaleX(1); }

.btn-gold { color: #e7c874; }
.btn-gold::after { background: #e7c874; }
.btn-gold:hover { color: #e7c874; }

.btn-outline-white { color: rgba(255,255,255,0.78); }
.btn-outline-white::after { background: rgba(255,255,255,0.6); }
.btn-outline-white:hover { color: rgba(255,255,255,0.95); }

.btn-outline-gold { color: #e7c874; opacity: 0.82; }
.btn-outline-gold::after { background: #e7c874; }
.btn-outline-gold:hover { color: #e7c874; opacity: 1; }

.btn-gold-large {
    color: #e7c874;
    font-size: 1.05rem;
    letter-spacing: 4.5px;
}
.btn-gold-large::after { background: #e7c874; }
.btn-gold-large:hover { color: #e7c874; }

/* ─── HERO ─────────────────────────────────── */
.hero {
    position: relative;
    height: 100svh;
    min-height: 600px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}
.hero-bg {
    position: absolute;
    inset: 0;
    background-image: url('/img/hero-wine.jpg');
    background-size: cover;
    background-position: center 30%;
    transform: scale(1.08);
    transition: transform 12s ease-out;
    will-change: transform;
}
.hero:hover .hero-bg { transform: scale(1.03); }
.hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
        to bottom,
        rgba(8,6,4,0.35) 0%,
        rgba(8,6,4,0.20) 40%,
        rgba(8,6,4,0.78) 100%
    );
}
.hero-content {
    position: relative;
    z-index: 2;
    text-align: center;
    color: #fff;
    padding: 0 5vw;
    max-width: 920px;
    animation: heroIn 1.8s cubic-bezier(0.16,1,0.3,1) both;
}
@keyframes heroIn {
    from { opacity: 0; transform: translateY(36px); }
    to   { opacity: 1; transform: translateY(0); }
}
.hero-eyebrow {
    font-family: var(--font-sans-serif);
    font-size: 1.05rem;
    letter-spacing: 5px;
    text-transform: uppercase;
    color: var(--color-primary, #e7c874);
    margin: 0 0 2.5rem;
    display: block;
    opacity: 0.9;
}
.hero-title {
    font-family: var(--font-display);
    font-size: clamp(6rem, 14vw, 14rem);
    font-weight: 200;
    letter-spacing: 14px;
    text-transform: uppercase;
    margin: 0 0 2rem;
    line-height: 1;
    color: #fff;
}
.hero-rule {
    width: 60px;
    height: 1px;
    background: var(--color-primary, #e7c874);
    margin: 0 auto 3rem;
}
.hero-desc {
    font-family: var(--font-serif);
    font-size: clamp(1.7rem, 2.2vw, 2.1rem);
    font-weight: 300;
    font-style: italic;
    line-height: 1.8;
    color: rgba(255,255,255,0.72);
    margin: 0 auto 4rem;
    max-width: 660px;
}
.hero-actions {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
}
.hero-scroll {
    position: absolute;
    bottom: 2.5rem;
    left: 50%;
    transform: translateX(-50%);
    z-index: 2;
    color: rgba(255,255,255,0.35);
    animation: bounce 2.4s infinite;
    transition: color 0.3s;
    text-decoration: none;
}
.hero-scroll:hover { color: var(--color-primary, #e7c874); }
@keyframes bounce {
    0%,100% { transform: translateX(-50%) translateY(0); }
    55%      { transform: translateX(-50%) translateY(8px); }
}

/* ─── QUOTE CAROUSEL ────────────────────────── */
.quote-section {
    padding: min(10vw, 100px) 5vw min(8vw, 80px);
    display: flex;
    justify-content: center;
    background: var(--color-bg);
}
.quote-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    max-width: 780px;
    width: 100%;
    gap: 3rem;
    text-align: center;
}
.progress-ring-wrap {
    position: relative;
    width: 52px;
    height: 52px;
    flex-shrink: 0;
}
.progress-ring__bg {
    fill: none;
    stroke: var(--color-border);
    stroke-width: 1.5;
}
.progress-ring__circle {
    fill: none;
    stroke: var(--color-primary);
    stroke-width: 1.5;
    stroke-linecap: round;
    stroke-dasharray: 138.23; /* 2π×22 */
    stroke-dashoffset: 138.23;
    transform: rotate(-90deg);
    transform-origin: 26px 26px;
}
.progress-ring__count {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-size: 1.5rem;
    color: var(--color-base);
    opacity: 0.5;
}
.quote-list {
    list-style: none;
    margin: 0;
    padding: 0;
    position: relative;
    min-height: 280px;
    width: 100%;
}
.quote-item {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2.5rem;
    opacity: 0;
    transform: translateY(16px);
    transition: opacity 0.7s ease, transform 0.7s ease;
    pointer-events: none;
}
.quote-item.revealed {
    opacity: 1;
    transform: none;
    pointer-events: auto;
    position: relative;
}
.quote-text {
    font-family: var(--font-serif);
    font-size: clamp(1.9rem, 2.5vw, 2.5rem);
    font-style: italic;
    font-weight: 400;
    line-height: 1.82;
    color: var(--color-base);
    opacity: 0.88;
    margin: 0;
    padding: 0 1rem;
    border: none;
    quotes: none;
}
.quote-text::before, .quote-text::after { content: none; }
.quote-cite {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.55rem;
    font-style: normal;
}
.quote-score {
    font-family: var(--font-display);
    font-size: 3.5rem;
    font-weight: 300;
    color: var(--color-primary);
    letter-spacing: 4px;
    line-height: 1;
}
.quote-source {
    font-family: var(--font-sans-serif);
    font-size: 1.05rem;
    letter-spacing: 2.5px;
    text-transform: uppercase;
    color: var(--color-base);
    opacity: 0.52;
}
.quote-wine {
    font-family: var(--font-serif);
    font-size: 1.5rem;
    font-style: italic;
    color: var(--color-base);
    opacity: 0.38;
}
.quote-dots {
    display: flex;
    gap: 12px;
    align-items: center;
}
.quote-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-border);
    border: none;
    cursor: pointer;
    padding: 0;
    transition: background 0.3s, transform 0.3s;
}
.quote-dot.active {
    background: var(--color-primary);
    transform: scale(1.4);
}

/* ─── STATEMENT ─────────────────────────────── */
.statement-section {
    padding: min(14vw, 140px) 8vw;
    text-align: center;
}
.statement-inner {
    max-width: 820px;
    margin: 0 auto;
}
.statement-heading {
    font-family: var(--font-display);
    font-size: clamp(3.4rem, 5.5vw, 6.5rem);
    font-weight: 300;
    font-style: normal;
    line-height: 1.38;
    color: var(--color-base);
    margin: 0 0 3.5rem;
    letter-spacing: 1px;
}
.statement-heading em {
    font-style: italic;
    color: var(--color-primary);
}
.statement-body {
    font-family: var(--font-serif);
    font-size: clamp(1.7rem, 1.8vw, 2rem);
    line-height: 1.85;
    color: var(--color-base);
    opacity: 0.62;
    max-width: 640px;
    margin: 0 auto 3rem;
}

/* ─── FEATURED WINE ─────────────────────────── */
.feature-section {
    display: grid;
    grid-template-columns: 55% 1fr;
    min-height: 88vh;
    overflow: hidden;
}
.feature-img-wrap {
    position: relative;
    overflow: hidden;
}
.feature-img-inner {
    width: 100%;
    height: 100%;
    overflow: hidden;
}
.feature-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transform: scale(1.06);
    transition: transform 10s cubic-bezier(0.16,1,0.3,1);
    display: block;
}
.feature-section:hover .feature-img { transform: scale(1); }
.feature-score-badge {
    position: absolute;
    bottom: 3rem;
    left: 3rem;
    display: flex;
    align-items: baseline;
    gap: 4px;
    background: rgba(8,6,4,0.55);
    backdrop-filter: blur(12px) saturate(1.4);
    -webkit-backdrop-filter: blur(12px) saturate(1.4);
    padding: 1.2rem 2rem;
}
.feature-score-num {
    font-family: var(--font-display);
    font-size: 5.5rem;
    font-weight: 200;
    color: #e7c874;
    line-height: 1;
}
.feature-score-pts {
    font-family: var(--font-sans-serif);
    font-size: 1rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: rgba(231,200,116,0.65);
}
.feature-content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: min(8vw, 80px) min(7vw, 80px);
}
.feature-title {
    font-family: var(--font-display);
    font-size: clamp(3rem, 5vw, 5.5rem);
    font-weight: 300;
    letter-spacing: 3px;
    line-height: 1.12;
    color: var(--color-base);
    margin: 0 0 1.5rem;
    text-transform: uppercase;
}
.feature-meta {
    font-family: var(--font-serif);
    font-size: 1.5rem;
    font-style: italic;
    color: var(--color-base);
    opacity: 0.45;
    margin: 0 0 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
}
.feature-rule {
    width: 40px;
    height: 1px;
    background: var(--color-primary);
    margin-bottom: 2.5rem;
    opacity: 0.8;
}
.feature-desc {
    font-family: var(--font-serif);
    font-size: clamp(1.6rem, 1.8vw, 1.95rem);
    font-style: italic;
    line-height: 1.8;
    color: var(--color-base);
    opacity: 0.62;
    margin: 0 0 1.5rem;
}
.feature-critic {
    font-family: var(--font-sans-serif);
    font-size: 1.05rem;
    letter-spacing: 1.5px;
    color: var(--color-base);
    opacity: 0.5;
    margin: 0 0 3rem;
    text-transform: uppercase;
}
.feature-critic strong {
    color: var(--color-primary);
    font-weight: 500;
}
.feature-link {
    font-family: var(--font-sans-serif);
    font-size: 1.05rem;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--color-base);
    text-decoration: none;
    opacity: 0.55;
    transition: opacity 0.25s, color 0.25s;
    border-bottom: 1px solid var(--color-primary);
    padding-bottom: 3px;
    align-self: flex-start;
}
.feature-link:hover { opacity: 1; color: var(--color-primary); text-decoration: none; }

/* ─── COLLECTION CARD BUTTON RESET ──────────── */
.collection-card {
    background: none;
    border: none;
    cursor: pointer;
    text-align: left;
}

/* ─── COLLECTION ─────────────────────────────── */
.collection-section {
    padding: min(12vw, 120px) 5vw;
}
.collection-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: min(6vw, 60px);
    padding: 0 2vw;
}
.collection-title {
    font-family: var(--font-display);
    font-size: clamp(3.5rem, 5vw, 6rem);
    font-weight: 300;
    letter-spacing: 6px;
    text-transform: uppercase;
    color: var(--color-base);
    margin: 0;
}
.collection-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 4vw 3vw;
}
.collection-card {
    text-decoration: none;
    color: var(--color-base);
    display: flex;
    flex-direction: column;
    gap: 2rem;
}
.collection-card:hover { text-decoration: none; }
.collection-card-img-wrap {
    position: relative;
    overflow: hidden;
    aspect-ratio: 3/4;
}
.collection-card-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.9s cubic-bezier(0.16,1,0.3,1);
    display: block;
}
.collection-card:hover .collection-card-img { transform: scale(1.05); }
.collection-card-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(8,6,4,0.55) 0%, rgba(8,6,4,0.1) 60%, transparent 100%);
    opacity: 0;
    transition: opacity 0.4s;
}
.collection-card:hover .collection-card-overlay { opacity: 1; }
.collection-card-cta {
    position: absolute;
    bottom: 1.8rem;
    left: 50%;
    transform: translateX(-50%) translateY(8px);
    font-family: var(--font-sans-serif);
    font-size: 0.85rem;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: rgba(231,200,116,0.9);
    opacity: 0;
    transition: opacity 0.4s, transform 0.4s cubic-bezier(0.2,0,0,1);
    white-space: nowrap;
    pointer-events: none;
}
.collection-card:hover .collection-card-cta {
    opacity: 1;
    transform: translateX(-50%) translateY(0);
}
.collection-card-body {
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
}
.collection-card-varietal {
    font-family: var(--font-sans-serif);
    font-size: 0.9rem;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--color-primary);
    opacity: 0.85;
}
.collection-card-title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 2.8vw, 3rem);
    font-weight: 300;
    letter-spacing: 2px;
    text-transform: uppercase;
    margin: 0;
    line-height: 1.2;
    transition: color 0.25s;
}
.collection-card:hover .collection-card-title { color: var(--color-primary); }
.collection-card-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
}
.collection-card-vintage {
    font-family: var(--font-serif);
    font-size: 1.4rem;
    font-style: italic;
    opacity: 0.4;
}
.collection-card-score {
    font-family: var(--font-display);
    font-size: 1.6rem;
    color: var(--color-primary);
    font-weight: 300;
}

/* ─── PARALLAX ──────────────────────────────── */
.parallax-section {
    position: relative;
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}
.parallax-bg {
    position: absolute;
    inset: -20%;
    background-image: url('/img/vineyard-landscape.jpg');
    background-size: cover;
    background-position: center;
    will-change: transform;
}
.parallax-overlay {
    position: absolute;
    inset: 0;
    background: rgba(8,6,4,0.62);
}
.parallax-content {
    position: relative;
    z-index: 2;
    text-align: center;
    color: #fff;
    padding: 6vw;
    max-width: 680px;
}
.parallax-title {
    font-family: var(--font-display);
    font-size: clamp(4rem, 7vw, 8rem);
    font-weight: 200;
    line-height: 1.2;
    letter-spacing: 3px;
    margin: 0 0 2.5rem;
    color: #fff;
}
.parallax-title em { color: #e7c874; font-style: italic; }
.parallax-rule {
    width: 50px;
    height: 1px;
    background: #e7c874;
    margin: 0 auto 2.5rem;
    opacity: 0.7;
}
.parallax-text {
    font-family: var(--font-serif);
    font-size: clamp(1.7rem, 2vw, 2rem);
    font-style: italic;
    line-height: 1.8;
    color: rgba(255,255,255,0.68);
    margin: 0 0 3.5rem;
}

/* ─── EVENTS ─────────────────────────────────── */
.events-section {
    padding: min(12vw, 120px) 5vw;
}
.events-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: min(6vw, 60px);
    padding: 0 2vw;
}
.events-title {
    font-family: var(--font-display);
    font-size: clamp(3.5rem, 5vw, 6rem);
    font-weight: 300;
    letter-spacing: 6px;
    text-transform: uppercase;
    color: var(--color-base);
    margin: 0;
}
.events-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 5vw 4vw;
}
.event-card {
    text-decoration: none;
    color: var(--color-base);
    display: flex;
    flex-direction: column;
    gap: 2rem;
}
.event-card:hover { text-decoration: none; }
.event-card-img-wrap {
    overflow: hidden;
    aspect-ratio: 16/10;
}
.event-card-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.9s cubic-bezier(0.16,1,0.3,1);
    display: block;
}
.event-card:hover .event-card-img { transform: scale(1.05); }
.event-card-body {
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
}
.event-card-label {
    font-family: var(--font-sans-serif);
    font-size: 0.9rem;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--color-primary);
    opacity: 0.85;
}
.event-card-title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 2.5vw, 2.8rem);
    font-weight: 300;
    letter-spacing: 1px;
    text-transform: uppercase;
    margin: 0;
    line-height: 1.25;
    transition: color 0.25s;
}
.event-card:hover .event-card-title { color: var(--color-primary); }
.event-card-desc {
    font-family: var(--font-serif);
    font-size: 1.5rem;
    font-style: italic;
    line-height: 1.7;
    opacity: 0.55;
    margin: 0;
}
.event-card-link {
    font-family: var(--font-sans-serif);
    font-size: 1rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    opacity: 0.45;
    transition: opacity 0.25s, color 0.25s;
    color: var(--color-primary);
    margin-top: 0.5rem;
}
.event-card:hover .event-card-link { opacity: 0.9; }

/* ─── JOIN THE LIST ──────────────────────────── */
.join-section {
    background: #0a0604;
    padding: min(14vw, 140px) 6vw;
    text-align: center;
}
.join-inner { max-width: 640px; margin: 0 auto; }
.join-title {
    font-family: var(--font-display);
    font-size: clamp(5rem, 8vw, 9rem);
    font-weight: 200;
    letter-spacing: 8px;
    text-transform: uppercase;
    color: #f0ead8;
    margin: 0 0 2rem;
    line-height: 1;
}
.join-rule {
    width: 50px;
    height: 1px;
    background: #e7c874;
    margin: 0 auto 3rem;
    opacity: 0.6;
}
.join-text {
    font-family: var(--font-serif);
    font-size: clamp(1.7rem, 2vw, 2rem);
    font-style: italic;
    line-height: 1.85;
    color: rgba(240,234,216,0.6);
    margin: 0 0 4rem;
}
.join-section .eyebrow { color: #e7c874; }

/* ─── MOBILE RESPONSIVE ──────────────────────── */
@media (max-width: 900px) {
    .feature-section {
        grid-template-columns: 1fr;
        min-height: auto;
    }
    .feature-img-wrap { height: 55vw; min-height: 320px; }
    .feature-img { object-fit: cover; }
    .feature-content { padding: 6vw 5vw; }
    .collection-grid { grid-template-columns: 1fr 1fr; gap: 5vw 4vw; }
    .events-grid { grid-template-columns: 1fr 1fr; gap: 6vw 4vw; }
}
@media (max-width: 600px) {
    .hero-title { letter-spacing: 8px; }
    .hero-actions { flex-direction: column; align-items: center; }
    .collection-grid { grid-template-columns: 1fr; }
    .events-grid { grid-template-columns: 1fr; }
    .collection-header, .events-header {
        flex-direction: column;
        gap: 1rem;
    }
    .statement-heading { font-size: clamp(2.8rem, 8vw, 4rem); }
    .feature-score-badge { bottom: 1.5rem; left: 1.5rem; }
    .feature-score-num { font-size: 4rem; }
    .parallax-title { font-size: clamp(3.5rem, 10vw, 6rem); }
    .join-title { font-size: clamp(4rem, 12vw, 7rem); }
}

/* ─── WINE MODAL ─────────────────────────────── */
.wine-modal {
    position: fixed;
    inset: 0;
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 4vw;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.35s cubic-bezier(0.2,0,0,1);
}
.wine-modal.is-open {
    pointer-events: auto;
    opacity: 1;
}
.wine-modal-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(4,2,0,0.82);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
}
.wine-modal-panel {
    position: relative;
    z-index: 2;
    display: grid;
    grid-template-columns: 45% 1fr;
    max-width: 960px;
    width: 100%;
    max-height: 90vh;
    background: var(--color-bg);
    overflow: hidden;
    transform: translateY(32px) scale(0.97);
    transition: transform 0.4s cubic-bezier(0.16,1,0.3,1);
}
.wine-modal.is-open .wine-modal-panel {
    transform: translateY(0) scale(1);
}
[data-theme='dark'] .wine-modal-panel {
    background: #100c08;
}
.wine-modal-close {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    z-index: 10;
    background: rgba(0,0,0,0.35);
    border: 1px solid rgba(255,255,255,0.15);
    color: rgba(255,255,255,0.8);
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    border-radius: 50%;
    transition: background 0.2s, color 0.2s;
}
.wine-modal-close:hover {
    background: rgba(231,200,116,0.2);
    color: #e7c874;
    border-color: rgba(231,200,116,0.4);
}
.wine-modal-img-wrap {
    position: relative;
    overflow: hidden;
}
.wine-modal-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    min-height: 500px;
}
.wine-modal-img-fade {
    position: absolute;
    inset: 0;
    background: linear-gradient(to right, transparent 60%, var(--color-bg) 100%);
}
[data-theme='dark'] .wine-modal-img-fade {
    background: linear-gradient(to right, transparent 60%, #100c08 100%);
}
.wine-modal-score-badge {
    position: absolute;
    bottom: 2rem;
    left: 2rem;
    display: flex;
    align-items: baseline;
    gap: 4px;
    background: rgba(8,6,4,0.65);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    padding: 1rem 1.6rem;
}
.wine-modal-score-num {
    font-family: var(--font-display);
    font-size: 4rem;
    font-weight: 200;
    color: #e7c874;
    line-height: 1;
}
.wine-modal-score-pts {
    font-family: var(--font-sans-serif);
    font-size: 0.85rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: rgba(231,200,116,0.6);
}
.wine-modal-content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 3.5rem 3rem 3.5rem 2.5rem;
    overflow-y: auto;
}
.wine-modal-varietal {
    font-family: var(--font-sans-serif);
    font-size: 0.85rem;
    letter-spacing: 3.5px;
    text-transform: uppercase;
    color: var(--color-primary);
    display: block;
    margin-bottom: 1.2rem;
    opacity: 0.9;
}
.wine-modal-title {
    font-family: var(--font-display);
    font-size: clamp(2.2rem, 3.5vw, 3.5rem);
    font-weight: 300;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--color-base);
    margin: 0 0 0.8rem;
    line-height: 1.1;
}
.wine-modal-meta {
    font-family: var(--font-serif);
    font-size: 1.35rem;
    font-style: italic;
    color: var(--color-base);
    opacity: 0.42;
    margin: 0 0 1.5rem;
}
.wine-modal-rule {
    width: 36px;
    height: 1px;
    background: var(--color-primary);
    margin-bottom: 2rem;
    opacity: 0.7;
}
.wine-modal-desc {
    font-family: var(--font-serif);
    font-size: clamp(1.5rem, 1.6vw, 1.75rem);
    font-style: italic;
    line-height: 1.78;
    color: var(--color-base);
    opacity: 0.58;
    margin: 0 0 1.8rem;
}
.wine-modal-notes {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 0.7rem;
    list-style: none;
    margin: 0 0 2.5rem;
    padding: 0;
}
.wine-modal-notes li {
    font-family: var(--font-sans-serif);
    font-size: 0.8rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--color-base);
    opacity: 0.45;
    padding: 4px 10px;
    border: 1px solid var(--color-border);
}
.wine-modal-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: auto;
}
.wine-modal-price {
    font-family: var(--font-display);
    font-size: 2.2rem;
    font-weight: 300;
    color: var(--color-base);
    opacity: 0.5;
}
.wine-modal-link {
    font-family: var(--font-sans-serif);
    font-size: 0.9rem;
    letter-spacing: 2.5px;
    text-transform: uppercase;
    color: var(--color-base);
    text-decoration: none;
    opacity: 0.5;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    position: relative;
    padding-bottom: 4px;
    transition: opacity 0.25s, color 0.25s, gap 0.35s cubic-bezier(0.2,0,0,1);
}
.wine-modal-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 1px;
    background: var(--color-primary);
    transform: scaleX(0.3);
    transform-origin: left;
    transition: transform 0.4s cubic-bezier(0.2,0,0,1);
}
.wine-modal-link:hover {
    opacity: 1;
    color: var(--color-primary);
    text-decoration: none;
    gap: 0.9rem;
}
.wine-modal-link:hover::after { transform: scaleX(1); }
.wine-modal-arrow { transition: transform 0.35s cubic-bezier(0.2,0,0,1); }
.wine-modal-link:hover .wine-modal-arrow { transform: translateX(4px); }

@media (max-width: 700px) {
    .wine-modal { padding: 0; align-items: flex-end; }
    .wine-modal-panel {
        grid-template-columns: 1fr;
        max-height: 92vh;
        border-bottom-left-radius: 0;
        border-bottom-right-radius: 0;
    }
    .wine-modal-img-wrap { height: 50vw; min-height: 220px; }
    .wine-modal-img { min-height: 220px; }
    .wine-modal-img-fade { background: linear-gradient(to top, var(--color-bg) 0%, transparent 60%); }
    [data-theme='dark'] .wine-modal-img-fade { background: linear-gradient(to top, #100c08 0%, transparent 60%); }
    .wine-modal-content { padding: 2.5rem 2rem 3rem; }
    .wine-modal-score-badge { display: none; }
}
</style>

<script>
// Quote carousel
(function() {
    const DURATION = 7500;
    const circumference = 2 * Math.PI * 22; // r=22

    let current = 0;
    let startTs: number | null = null;
    let paused = false;
    let rafId: number;

    const items = Array.from(document.querySelectorAll<HTMLElement>('.quote-item'));
    const dots  = Array.from(document.querySelectorAll<HTMLButtonElement>('.quote-dot'));
    const ring  = document.querySelector<SVGCircleElement>('.progress-ring__circle');
    const countEl = document.getElementById('quote-count');
    const section = document.getElementById('quote-section');

    if (!items.length || !ring) return;

    function goTo(idx: number) {
        items[current].classList.remove('revealed');
        dots[current]?.classList.remove('active');
        current = ((idx % items.length) + items.length) % items.length;
        items[current].classList.add('revealed');
        dots[current]?.classList.add('active');
        if (countEl) countEl.textContent = String(current + 1);
        startTs = null;
    }

    function tick(ts: number) {
        if (!paused) {
            if (!startTs) startTs = ts;
            const p = Math.min((ts - startTs) / DURATION, 1);
            ring!.style.strokeDashoffset = String(circumference * (1 - p));
            if (p >= 1) goTo(current + 1);
        }
        rafId = requestAnimationFrame(tick);
    }

    dots.forEach((d, i) => d.addEventListener('click', () => goTo(i)));

    // Touch swipe support
    let touchX = 0;
    section?.addEventListener('touchstart', (e) => { touchX = e.touches[0].clientX; }, { passive: true });
    section?.addEventListener('touchend', (e) => {
        const dx = e.changedTouches[0].clientX - touchX;
        if (Math.abs(dx) > 40) goTo(dx < 0 ? current + 1 : current - 1);
    }, { passive: true });

    rafId = requestAnimationFrame(tick);
    document.addEventListener('astro:before-swap', () => cancelAnimationFrame(rafId));
})();

// Wine card modal
(function() {
    const modal = document.getElementById('wine-modal') as HTMLElement;
    if (!modal) return;

    const backdrop = modal.querySelector('.wine-modal-backdrop') as HTMLElement;
    const closeBtn = modal.querySelector('.wine-modal-close') as HTMLElement;
    const img = document.getElementById('modal-img') as HTMLImageElement;
    const scoreNum = document.getElementById('modal-score-num') as HTMLElement;
    const varietal = document.getElementById('modal-varietal') as HTMLElement;
    const title = document.getElementById('modal-title') as HTMLElement;
    const meta = document.getElementById('modal-meta') as HTMLElement;
    const desc = document.getElementById('modal-desc') as HTMLElement;
    const notes = document.getElementById('modal-notes') as HTMLElement;
    const price = document.getElementById('modal-price') as HTMLElement;
    const link = document.getElementById('modal-link') as HTMLAnchorElement;
    const scoreBadge = modal.querySelector('.wine-modal-score-badge') as HTMLElement;

    function openModal(trigger: HTMLElement) {
        const slug = trigger.dataset.wineSlug || '';
        if (img) { img.src = trigger.dataset.wineImg || ''; img.alt = trigger.dataset.wineTitle || ''; }
        if (scoreNum) scoreNum.textContent = trigger.dataset.wineRating || '';
        if (scoreBadge) scoreBadge.style.display = trigger.dataset.wineRating ? '' : 'none';
        if (varietal) varietal.textContent = trigger.dataset.wineVarietal || '';
        if (title) title.textContent = trigger.dataset.wineTitle || '';

        const parts = [trigger.dataset.wineAppellation, trigger.dataset.wineVintage].filter(Boolean);
        if (meta) meta.textContent = parts.join(' · ');

        if (desc) desc.textContent = trigger.dataset.wineDescription || '';

        if (notes) {
            notes.innerHTML = '';
            try {
                const noteList: string[] = JSON.parse(trigger.dataset.wineNotes || '[]');
                noteList.forEach(n => {
                    const li = document.createElement('li');
                    li.textContent = n;
                    notes.appendChild(li);
                });
            } catch {}
        }

        if (price) price.textContent = trigger.dataset.winePrice || '';
        if (link) link.href = `/work/${slug}`;

        modal.classList.add('is-open');
        modal.setAttribute('aria-hidden', 'false');
        document.body.style.overflow = 'hidden';
    }

    function closeModal() {
        modal.classList.remove('is-open');
        modal.setAttribute('aria-hidden', 'true');
        document.body.style.overflow = '';
    }

    document.querySelectorAll<HTMLElement>('[data-wine-slug]').forEach(trigger => {
        trigger.addEventListener('click', () => openModal(trigger));
    });

    backdrop?.addEventListener('click', closeModal);
    closeBtn?.addEventListener('click', closeModal);
    document.addEventListener('keydown', (e) => { if (e.key === 'Escape') closeModal(); });
    document.addEventListener('astro:before-swap', () => { closeModal(); });
})();

// Parallax scroll on vineyard section
(function() {
    const bg = document.getElementById('parallax-bg') as HTMLElement;
    if (!bg) return;
    const onScroll = () => {
        const section = bg.parentElement!;
        const rect = section.getBoundingClientRect();
        const pct = (window.innerHeight - rect.top) / (window.innerHeight + rect.height);
        const shift = (pct - 0.5) * 80;
        bg.style.transform = `translateY(${shift}px)`;
    };
    window.addEventListener('scroll', onScroll, { passive: true });
    document.addEventListener('astro:before-swap', () =>
        window.removeEventListener('scroll', onScroll)
    );
})();
</script>
